# Weekly Review — YYYY-Www

## The numbers

Rows are per-chart: one line per metric this athlete's `goals.md` domains actually name, and
nothing else. A row nobody's goals need is a chore the coach invented (CLAUDE.md §1.1).

| Metric | Target | Actual | Delta |
|---|---|---|---|
| Sessions completed | | | |
| | | | |

## Trend
- Primary metric: this week's mean vs. last week, and vs. four weeks ago
- On/off projected trajectory:
- Strength markers, where a strength domain exists — reps at a **fixed** load and a **fixed**
  RIR, not e1RM. Testing a true max to satisfy curiosity is on CLAUDE.md §2's pushback list.
  - Markers actually re-tested this week (against the list in `goals.md`):

## Diagnosis
- What worked:
- What didn't, and why (mechanism, not blame):
- Adherence, **per dial** — a single percentage hides which dial failed, and the dial that
  failed is the whole diagnosis. One entry per dial this chart runs: sessions vs the block
  floor __ · sessions vs the week as intended __ · plus whichever of steps, protein, calorie
  ceiling or symptom-log completion this athlete's plan actually asks for.
  **If any dial is below `plan.adherenceRoutingPct` (`athlete/constants.json`), route to the
  adherence agent, not to nutrition or strength.** Cite the key, never retype the number —
  `scripts/test-single-home.mjs` checks every document that states it against that one home.
- Framing check (`athlete/values.md`): state a cost as "this costs you about X weeks", never as
  "you should cut back". An exchange rate is something the athlete can spend; an instruction is
  something they can only obey or fail.

## Ships with (CLAUDE.md §2 — all three are mandatory)
- Strongest counterargument, written by the coach:
- Confidence (high / moderate / speculative), and what would change it:
- Failure mode — this falls apart when:

## Verdict
- [ ] Hold — change nothing (this is the correct call most weeks)
- [ ] Adjust one variable: ___
- [ ] Deload / diet break
- [ ] Escalate — something here needs a doctor

## Next week
- The one thing that matters:
